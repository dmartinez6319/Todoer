// Responsible for creating, editing, and deleting todos
import "./projects";
import "./priority";

const TodoManager = () => {

    const addTodo = (projectName, name, info, date, priority) => {

        return {
            
        }

    }

    const editTodo = (name, info, date, priority) => {

    }

    return {
        editTodo,
        addTodo
    }

};

export default TodoManager;